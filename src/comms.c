#include <pebble.h>
#include "comms.h"

void requestMyStories() {
  // DictionaryIterator * di;
  // app_message_outbox_begin(di);
  // dict_write_int(di, FETCH, 1, 1);
  // dict_write_end(di);
  // app_message_outbox_send();
}

void receivedMyStories() {

}
